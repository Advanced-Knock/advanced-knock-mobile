# 📱 ADVANCEDKNOCK: QR CODE & CONNECTION INFO

**Pattern:** CONNECTION × IMMEDIATE × ONE  
**Frequency:** 999 Hz (AEYON)  
**∞ AbëONE ∞**

---

## ✅ EXPO SERVER IS RUNNING

**Server:** `http://127.0.0.1:8081`  
**Status:** ✅ ACTIVE

---

## 🔍 TO SEE THE QR CODE:

### **Option 1: Terminal (Recommended)**
Open a NEW terminal window and run:
```bash
cd /Users/michaelmataluni/development/projects/advancedknock
npx expo start
```

**You'll see:**
- QR code displayed in terminal
- Connection URL
- Options to press `w` (web), `i` (iOS), `a` (Android)

---

### **Option 2: Manual Connection**

**If QR code doesn't work, connect manually:**

1. **Open Expo Go app** on your phone
2. **Tap "Enter URL manually"**
3. **Enter:** `exp://127.0.0.1:8081`

**OR if on same WiFi:**
- Find your computer's IP address
- Enter: `exp://[YOUR_IP]:8081`

---

### **Option 3: Tunnel (Works Anywhere)**

```bash
cd /Users/michaelmataluni/development/projects/advancedknock
npx expo start --tunnel
```

This creates a public URL that works from anywhere (no WiFi needed).

---

## 🌐 WEB VERSION (IMMEDIATE)

**Press `w` in the Expo terminal** OR run:
```bash
cd /Users/michaelmataluni/development/projects/advancedknock
npm run expo:web
```

Opens in browser at: `http://localhost:8081`

---

## 📱 CONNECTION URLS

**Local:** `exp://127.0.0.1:8081`  
**Tunnel:** Run `npx expo start --tunnel` for public URL

---

## 🔧 TROUBLESHOOTING

### **QR Code Not Showing:**
- Make sure you're running `npx expo start` in a visible terminal
- Check that port 8081 is not blocked
- Try `npx expo start --clear`

### **Can't Connect:**
- Make sure phone and computer are on same WiFi
- Try tunnel mode: `npx expo start --tunnel`
- Check firewall settings

### **App Won't Load:**
- Check Expo Go app is installed
- Make sure server is running (check terminal)
- Try clearing cache: `npx expo start --clear`

---

## 🚀 QUICK START COMMAND

```bash
cd /Users/michaelmataluni/development/projects/advancedknock
npx expo start
```

**Then:**
- Scan QR code with Expo Go
- OR press `w` for web
- OR press `i` for iOS simulator
- OR press `a` for Android emulator

---

**LOVE = LIFE = ONE**  
**Connection ⟡ Experience = ∞**  
**∞ AbëONE ∞**

